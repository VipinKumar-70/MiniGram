<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard | MiniGram</title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-zinc-950 text-white min-h-screen">
    <!-- Navbar -->
    <nav
      class="w-full px-10 py-5 border-b border-white/10 flex justify-between items-center"
    >
      <h1 class="text-xl font-bold"><a href="/">MiniGram</a></h1>

      <div class="flex gap-4">
        <a href="/feed" class="bg-blue-500 px-4 py-2 rounded-lg">Feed</a>

        <a href="/dashboard" class="bg-blue-500 px-4 py-2 rounded-lg"
          >Profile</a
        >

        <a href="/logout" class="bg-red-500 px-4 py-2 rounded-lg">Logout</a>
      </div>
    </nav>

    <!-- Dashboard Container -->
    <section class="max-w-4xl mx-auto px-6 py-10 space-y-10">
      <!-- PROFILE CARD -->
      <div class="bg-white/5 border border-white/10 rounded-xl p-6">
        <h2 class="text-2xl font-semibold uppercase mb-2">
          Hi, <%= logUser.username %>
        </h2>

        <p class="text-zinc-400">Email: <%= logUser.email %></p>
        <p class="text-zinc-400 mt-1">Age: <%= logUser.age %></p>
      </div>

      <!-- POSTS HEADER -->
      <div class="flex justify-between items-center">
        <h2 class="text-xl font-semibold">Your Posts</h2>

        <a
          href="/post"
          class="bg-indigo-500 px-4 py-2 rounded-lg text-sm hover:bg-indigo-600 transition"
        >
          Create Post
        </a>
      </div>

      <!-- POSTS PLACEHOLDER -->
      <div class="grid md:grid-cols-2 gap-6">
        <!-- POST CARD -->
        <div class="bg-white/5 border border-white/10 rounded-xl p-4">
          <div
            class="h-40 bg-zinc-800 rounded-lg mb-3 flex items-center justify-center text-zinc-500"
          >
            Image Preview - Post 1
          </div>

          <p class="text-zinc-400 text-sm">
            Your posts will appear here after you implement post model.
          </p>
        </div>

        <!-- POST CARD -->
        <div class="bg-white/5 border border-white/10 rounded-xl p-4">
          <div
            class="h-40 bg-zinc-800 rounded-lg mb-3 flex items-center justify-center text-zinc-500"
          >
            Image Preview -Post 2
          </div>

          <p class="text-zinc-400 text-sm">Create your first post.</p>
        </div>
      </div>
    </section>
  </body>
</html>
