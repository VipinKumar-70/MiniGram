<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard | MiniGram</title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-zinc-950 text-white min-h-screen">
    <!-- Navbar -->
    <nav
      class="w-full px-10 py-5 border-b border-white/10 flex justify-between items-center"
    >
      <h1 class="text-2xl font-bold">MiniGram</h1>

      <a
        href="/logout"
        class="px-4 py-2 bg-red-500 rounded-lg hover:bg-red-600 transition"
      >
        Logout
      </a>
    </nav>

    <!-- Dashboard Container -->
    <section class="max-w-6xl mx-auto px-6 py-10">
      <!-- User Card -->
      <div
        class="bg-white/5 border border-white/10 rounded-xl p-6 flex justify-between items-center"
      >
        <div>
          <h2 class="text-xl font-semibold"><%= logUser.username %></h2>
          <p class="text-zinc-400"><%= logUser.email %></p>
          <p class="text-zinc-400">Age: <%= logUser.age %></p>
        </div>

        <a
          href="/create-post"
          class="px-5 py-2 bg-blue-600 rounded-lg hover:bg-blue-700 transition"
        >
          + New Post
        </a>
      </div>
    </section>
  </body>
</html>
